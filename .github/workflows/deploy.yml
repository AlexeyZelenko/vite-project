name: Deploying to Firebase
on: [push]
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello GH Actions!
      - name: Print few to lines
        run: echo Actions!
